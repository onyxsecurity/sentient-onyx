# PRDCT-12580 — PR evidence (images and screen recordings only)

Embedded in onyxsecurity/onyx-agent-gateway#106 so the proof renders for a reviewer.

Deliberately contains ONLY screenshots and screen recordings, all checked to carry no
credential material. Raw Playwright traces are NOT published here — they capture request
headers and belong only in the run's private artifact directory.
