# Cost-opt route-binding guard rail — PR evidence (images only)

Screenshots only, checked to carry no credential material. The credential ids visible
are public identifiers of throwaway keys revoked immediately after capture; no key
secret, session id or token appears here. Raw Playwright traces are NOT published --
they capture request headers and stay in the run's private artifact directory.
